# Agent Instructions

## Git workflow

- Do not create or switch to a new Git branch unless the user explicitly asks for branch creation.
- For commit and push requests, use the current branch unless the user explicitly requests a different branch.
